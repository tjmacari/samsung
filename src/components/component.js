import React from 'react';

/**
 * We can add useful methods to this class in future to avoid redundant coding in child classes
 */
class TmComponent extends React.Component {
    constructor(props) {
        super(props);
    }
}
export default TmComponent;
